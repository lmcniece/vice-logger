import generateChartData from 'vice-logger/utils/generate-chart-data';
import { module, test } from 'qunit';

module('Unit | Utility | generate chart data');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = generateChartData();
  assert.ok(result);
});
